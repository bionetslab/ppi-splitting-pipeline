name: ppi-splitting-pipeline
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.14
  - certifi
  - blast
  - cd-hit
  - kahip
  - multiqc
  - scikit-learn
  - numpy
  - matplotlib
  - plotly
  - pyscipopt
  - pip:
    - torch
    - transformers
    - sentencepiece
    - protobuf
    - cvxpy[GUROBI]
    - highspy>=1.7      # OSS MIP fallback for sample_negatives_ilp.py when Gurobi is unavailable
    - pyyaml            # --config YAML for sample_negatives_ilp.py --batch
